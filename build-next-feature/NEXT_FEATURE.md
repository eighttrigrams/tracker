On the messages we receive, add a text field for annotation,
which we can edit with exactly one annotation text.

The editing UI works exactly like for tasks,
and the annotations should be persisted.

It seems to me this can be just another field on the messages table.