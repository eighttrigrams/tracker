The backend search string concatenation on the tasks search.

Introduce honey sql that way you can work with proper data structures.

Dont change the tests in this refactoring, since they are meant to guard this refactoring.

Important: we don't do a full refactor of everything. This stage is more for feasability, getting 
us started with a multi-day refctoring, and determine how resilient and stable our app is
after we make the change. So refactor a couple of functions and then stop and leave the rest for later.