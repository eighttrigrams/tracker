Implement dark mode.

The button should be between the work/private switcher (if shown) and the login group

It should featuer a moon,sun symbol.

We don't need to persist the state across reloads.

We want an animation transitioning the colors over 2 seconds.